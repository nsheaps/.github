name: "Setup Environment"
description: "Set up tools via mise, corepack, and install dependencies"

runs:
  using: "composite"
  steps:
    - name: Setup mise
      uses: jdx/mise-action@v2
      with:
        install: true

    - name: Enable and install corepack
      shell: bash
      run: |
        mise exec -- corepack enable
        mise exec -- corepack install

    - name: Install dependencies
      shell: bash
      run: mise exec -- yarn install
