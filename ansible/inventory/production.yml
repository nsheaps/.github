---
# Production inventory for file synchronization
# Minimal inventory - actual target repos are defined in config/sync-tasks.yml

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
